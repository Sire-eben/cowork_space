import 'package:flutter/material.dart';

class BookingDesktop extends StatelessWidget {
  const BookingDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('Booking page')]);
  }
}
